Add new rsvp to list endpoint.
    add a new RSVP to an event using a key 
    it takes in an email and the id for the event
---
tags:
  - Bright Events API
parameters:
  - in: path
    name: key
    required: true
    description: The ID of the Event, try 1!
    type: string
  - in: formData
    name: email
    type: string
    description: The email to be added!
    required: true
responses:
  201:
    description: 
    schema:
      $ref: '#/definitions/Task'
    examples:
        "email": "frank@frank.com"